// The main function is always the first code that runs in every executable Rust program
fn main() {
    println!("Hello, world!"); // the ! indicates that a rust macro was called instead of a function
}

// Use the command: rustc main.rs to compile the program 
// and ./main to run it  